# Changelog

Repo-wide, lockstep across every package in `packages/`. Per-package detail
lives in each package's own `CHANGELOG.md`.

## 0.1.0

- Repo scaffolded with `packages/core`. No features extracted yet.
