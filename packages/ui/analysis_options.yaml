include: package:flutter_lints/flutter.yaml

# Files copied for review but not yet reconciled — still import
# package:booking_application directly (see docs/PENDING.md). Excluded
# rather than fixed so the analyzer only reports on what's actually meant
# to work right now; remove each line as its file is reconciled and wired
# into bookie_buddy_ui.dart.
analyzer:
  exclude:
    - lib/features/thermal_printer/presentation/receipt_design/shared/receipt_date_formatter.dart
    - lib/features/thermal_printer/presentation/receipt_design/shared/receipt_shared_sections.dart
    - lib/features/thermal_printer/presentation/receipt_design/shared/shop_receipt_sections.dart
    - lib/core/constants/enums/main_service_type_enums.dart
    - lib/utils/helpers/product_field_helper.dart
    - lib/features/thermal_printer/presentation/receipt_design/builders/booking_receipt_canvas_builder.dart
    - lib/features/thermal_printer/presentation/receipt_design/builders/sales_receipt_canvas_builder.dart
